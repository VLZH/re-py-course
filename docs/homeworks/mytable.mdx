---
id: mytable
title: Моя таблица
---

Задача простая будет дополняться
 
### Первые шаги
- Создать базу данных внутри Docker контейнера
- Создать  в ней таблицу mytable с полями

```python
id #SERIAL PRIMARY KEY
name # TEXT
surname # TEXT
hobby # TEXT
сode # INT
burn_date # DATE
```
- Создать в таблице запись с заполнением всех полей
### Подсказки 

- Список докер контейнеров
```bash
docker ps  #список запущенных контейнеров
docker ps -a # список всех контейнеров
```
- Запуск докер контейнера
```bash
docker start container_id
```
- Зайти в докер контейнер
```bash
docker exec -it container_id bash
```
- Внутри докера зайти в postgresql
```bash
psql user=postgres
```
Как создать базу данных и таблицу есть в разделе DB этого курса. 
Советую создавать базу данных и таблицу именно через терминал для опыта но можнo также через DBeaver.
Задачи будут проверяться онлайн через Discord









